[pytest]
# tests/ на sys.path -> itest_config / itest_helpers импортируются по имени.
pythonpath = tests
addopts = -ra -q
markers =
    unit: быстрые тесты без инфраструктуры
    integration: требуют поднятого стека (docker compose)
    e2e: сквозной пользовательский сценарий через API
